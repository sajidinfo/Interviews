
## 🔥 Angular Interview Top Questions

### 🔹 Components / Templates

1. Difference between **Component** and **Directive**?
2. Agar hum `<app-login>hii</app-login>` likhe aur component me `<ng-content>` ho ya na ho, to output kya hoga? (jo humne abhi discuss kiya).
3. `ChangeDetectionStrategy.OnPush` kya hai, kyu use karte hain?
4. `trackBy` function `*ngFor` me kyu use karte hain? Without `trackBy` kya problem hoti hai?
5. Difference between **template-driven forms** and **reactive forms**?
6. Angular me `ViewChild` aur `ContentChild` me kya farq hai?
7. `ngOnInit` vs `constructor` – dono me difference kya hai?

---

### 🔹 Services / Dependency Injection

8. Service ko `NgModule` me add kiye bina use kar sakte hain kya? Agar haan, kaise?
9. Difference between **providedIn: 'root'** aur manually module me provide karna?
10. Lazy-loaded module me service scope kaise change hota hai?

---

### 🔹 RxJS & State

11. Difference between **Subject**, **BehaviorSubject**, and **ReplaySubject**?
12. `async pipe` kya karta hai?
13. `switchMap`, `mergeMap`, `concatMap`, `exhaustMap` ke use cases kya hain?

---

### 🔹 Performance

14. Angular app me **performance optimize** kaise karoge? (jitne points bata sakte ho)
15. Browser ke **Chrome DevTools Performance tab** kaise use karte ho? (jo tumne abhi pucha tha 👌)
16. Angular Universal (SSR) ka use kyu hota hai SEO me?

---

### 🔹 Security

17. XSS (Cross-Site Scripting) attack se Angular app ko kaise secure karoge?
18. Angular automatic CSRF protection kaise deta hai?
19. CORS kya hota hai, aur Angular/Node me ise kaise handle karte ho?

---

### 🔹 Routing

20. Lazy loading aur eager loading modules me difference kya hai?
21. Route guards ke types kya hote hain (`CanActivate`, `CanDeactivate`, etc.)?
22. Angular me **preloading strategies** kya hoti hain?

---

### 🔹 Testing

23. Angular component ko Jasmine/Karma se kaise test karte ho?
24. `TestBed` ka use kyu karte hain? Kab bina `TestBed` ke test likh sakte ho?
25. `beforeEach`, `it`, `expect` ka use interview style me samjhao.

---

### 🔹 Advanced / Misc

26. Angular lifecycle hooks ko order me explain karo (`ngOnInit`, `ngAfterViewInit`, etc.)
27. `ngZone` kya hai, aur kab manually use karte ho?
28. Angular me internationalization (i18n) kaise implement karte ho?
29. LTR/RTL (left-to-right / right-to-left) support Angular me kaise setup karoge?
30. Webpack aur Angular CLI ke beech kya relation hai?


---

## 🔥 Extra Angular Interview Questions (Advance + Practical)

### 🔹 Angular Basics & Core

1. AngularJS (v1) aur Angular (2+) me kya differences hain?
2. Angular ke modules ka purpose kya hai (`NgModule`)?
3. `CommonModule` aur `BrowserModule` me difference kya hai?
4. Angular me polyfills ka role kya hai?
5. Angular compiler ke 2 modes: JiT (Just-in-Time) vs AoT (Ahead-of-Time) — difference?

---

### 🔹 Lifecycle & Rendering

6. Angular lifecycle hooks ko real use cases ke sath explain karo.
7. Agar tumhare paas `ngOnChanges` aur `ngOnInit` dono hain, kaunsa kab trigger hoga?
8. DOM rendering me `Renderer2` ka use kyu hota hai? Direct DOM access kyu avoid karna chahiye?
9. Virtual scrolling kya hota hai? Angular CDK me iska use kyu karte hain?

---

### 🔹 Change Detection & Zone.js

10. Angular me change detection kaise kaam karta hai?
11. `NgZone` aur `zone.js` ka role kya hai?
12. Agar tumhe change detection manually trigger karni ho, to kaise karoge (`ChangeDetectorRef`) ?
13. Pure vs Impure pipes me difference kya hai? Performance impact?

---

### 🔹 Forms & Validation

14. Reactive forms me custom validator kaise banate ho?
15. Async validator kaise banate ho?
16. Reactive form aur template-driven form me code-level differences kya hote hain?

---

### 🔹 Routing & Navigation

17. Route parameters aur query parameters me difference?
18. Lazy loaded module me guard lagane ka process samjhao.
19. Angular router events (`NavigationStart`, `NavigationEnd`, etc.) ko kaise use karte ho?
20. `resolve` guard kaise use hota hai aur iska benefit kya hai?

---

### 🔹 HTTP & API

21. Angular HttpInterceptor kya hai aur kyu use karte ho?
22. Multiple interceptors ka order kaise decide hota hai?
23. Agar API me 401 Unauthorized aaye to automatically login page pe redirect kaise karoge?
24. Angular me `withCredentials: true` ka kya role hai?

---

### 🔹 RxJS & Async

25. RxJS me hot aur cold observables me difference?
26. `takeUntil` operator ka use kya hai?
27. Agar ek observable ko cancel karna ho to kaise karoge?
28. `forkJoin`, `combineLatest`, `zip`, aur `race` ke beech difference?
29. Error handling RxJS me kaise karte ho (`catchError`, `retry`, etc.)?

---

### 🔹 Performance & Optimization

30. Large Angular app me bundle size kaise reduce karoge? (Tree-shaking, lazy loading, code splitting, etc.)
31. Angular me service worker kaise setup karte ho (PWA ke liye)?
32. Image lazy-loading kaise karte ho Angular me?
33. Large list render karte waqt performance optimize karne ke liye kya techniques use karoge?

---

### 🔹 Security

34. Angular ka built-in **DomSanitizer** kya hai?
35. XSS ke examples aur Angular kaise prevent karta hai?
36. CSP (Content Security Policy) kya hota hai aur Angular app me isse kaise implement karte ho?

---

### 🔹 Testing & Debugging

37. Unit testing aur integration testing me difference kya hai Angular me?
38. Component me dependency injection test karte waqt mock service kaise banate ho?
39. Angular testing utilities me `fakeAsync` aur `async` ka difference?
40. Protractor aur Cypress me difference kya hai?

---

### 🔹 Miscellaneous

41. Angular Universal kya hai aur kaise implement karte ho SSR ke liye?
42. Angular elements (Web Components) kya hote hain?
43. Monorepo structure me Angular apps ko kaise manage karte ho (Nx, Lerna, etc.)?
44. Angular me i18n (internationalization) ke steps kya hote hain?
45. LTR aur RTL layout support Angular me kaise enable karte ho?

---


---
